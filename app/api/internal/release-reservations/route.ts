import{releaseExpiredReservations}from"../../../lib/inventory";import{ensureStore,runtimeEnv}from"../../../lib/store";export async function POST(request:Request){const secret=runtimeEnv().CRON_SECRET;if(!secret||request.headers.get("authorization")!==`Bearer ${secret}`)return new Response("No autorizado",{status:401});await ensureStore();await releaseExpiredReservations();return Response.json({ok:true})}