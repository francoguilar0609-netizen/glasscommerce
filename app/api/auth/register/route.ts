import {createAccount} from "../../../lib/auth";import{ensureStore}from"../../../lib/store";export async function POST(request:Request){try{await ensureStore();const body=await request.json() as {email?:string;password?:string};const result=await createAccount(request,body.email||"",body.password||"");return Response.json({user:result.user},{status:201,headers:{"Set-Cookie":result.setCookie}})}catch(error){if(error instanceof Response)return error;return Response.json({error:"No se pudo crear la cuenta."},{status:500})}}