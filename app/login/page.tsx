"use client";import{FormEvent,useState}from"react";import Link from"next/link";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(event:FormEvent,mode:"login"|"register"){event.preventDefault();setBusy(true);setError("");const response=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});setBusy(false);if(response.ok){window.location.assign(new URLSearchParams(location.search).get("return_to")||"/");return}const data=await response.json().catch(()=>({}));setError(data.error||await response.text()||"No se pudo autenticar.")}return <main className="admin-page"><Link className="brand" href="/"><span>G</span>GlassCommerce</Link><div className="eyebrow">ACCESO SEGURO</div><h1>Tu cuenta</h1><form className="inventory glass" onSubmit={e=>submit(e,"login")}><label>Correo<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Contraseña<input type="password" autoComplete="current-password" minLength={12} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p role="alert">{error}</p>}<button disabled={busy} type="submit">Iniciar sesión</button><button disabled={busy} type="button" onClick={e=>submit(e as unknown as FormEvent,"register")}>Crear cuenta</button></form></main>}