export type Product={id:number;name:string;slug:string;category:string;description:string;emoji:string;price_pen:number;stock:number;reserved_stock:number;active:number};
export type RuntimeEnv={DB:D1Database;MERCADO_PAGO_ACCESS_TOKEN?:string;MERCADO_PAGO_WEBHOOK_SECRET?:string;ADMIN_EMAILS?:string;USD_PEN_RATE?:string;TRUST_PROXY_AUTH_HEADERS?:string;TRUST_PROXY_AUTH_SECRET?:string;ALLOW_LOCAL_REGISTRATION?:string;CRON_SECRET?:string};
export function runtimeEnv(){const runtime=(globalThis as typeof globalThis&{__GLASSCOMMERCE_ENV?:RuntimeEnv}).__GLASSCOMMERCE_ENV;if(!runtime?.DB)throw new Error("La base de datos de la tienda no está disponible.");return runtime}
export async function ensureStore(){const{DB}=runtimeEnv();try{await DB.prepare("SELECT reserved_stock FROM products LIMIT 1").first();await DB.prepare("SELECT id FROM users LIMIT 1").first();await DB.prepare("SELECT payment_status,checkout_fingerprint FROM orders LIMIT 1").first()}catch{throw new Error("La base de datos no tiene aplicada la migración 0001_secure_auth_inventory.")}const count=await DB.prepare("SELECT COUNT(*) AS count FROM products").first<{count:number}>();if(!count?.count){const seed=[["Aurora Headphones","aurora-headphones","Audio","Sonido espacial, batería de 40 horas y cancelación adaptativa.","🎧",47900,18],["Halo Desk Light","halo-desk-light","Workspace","Iluminación regulable con control táctil y USB-C.","💡",29500,12],["Drift Keyboard","drift-keyboard","Workspace","Teclado mecánico inalámbrico de perfil bajo.","⌨️",40500,8],["Nova Speaker","nova-speaker","Audio","Audio estéreo portátil con cuerpo resistente.","🔊",55500,6],["Orbit Watch","orbit-watch","Wearables","Salud, cinco días de batería y pantalla siempre activa.","⌚",70500,14],["Prism Bottle","prism-bottle","Lifestyle","Acero térmico que conserva bebidas frías por 24 horas.","💧",14500,24]];await DB.batch(seed.map(p=>DB.prepare("INSERT INTO products(name,slug,category,description,emoji,price_pen,stock) VALUES(?,?,?,?,?,?,?)").bind(...p)))}}
export function orderId(){return`GC-${crypto.randomUUID().replaceAll("-","").slice(0,16).toUpperCase()}`}
